{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Project Pulse Dashboard",
      "type": "pwa-chrome",
      "request": "launch",
      "preLaunchTask": "Start Project Pulse dashboard server",
      "url": "http://localhost:5500/index.html",
      "webRoot": "${workspaceFolder}/app"
    }
  ]
}
